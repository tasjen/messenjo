# for generating AuthService gRPC-web code
# run: npx buf generate auth.proto

version: v1
plugins:
  - plugin: es
    out: lib/auth_proto2
    opt:
      - target=ts
  - plugin: connect-es
    out: lib/auth_proto2
    opt:
      - target=ts
