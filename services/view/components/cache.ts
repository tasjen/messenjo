// import { fetchContacts, fetchUserData } from "@/lib/data";
// import { cache } from "react";

// export const getContacts = cache(async () => await fetchContacts());

// export const getUser = cache(async () => await fetchUserData());
